/** Default local start time (Europe/Paris) for a club evening. */
export const DEFAULT_EVENT_START_TIME = "23:00";

/** Typical duration from doors open to close. */
export const DEFAULT_EVENT_DURATION_HOURS = 5;
